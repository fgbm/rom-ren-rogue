{
  "name": "neuromancer-rogue",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run lint && tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "node scripts/rom.ts lint",
    "stats": "node scripts/rom.ts stats",
    "graph": "node scripts/rom.ts graph",
    "simulate": "node scripts/simulate.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
